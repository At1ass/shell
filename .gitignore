# =============================================================================
#  shell (Quickshell / Hyprland) — .gitignore
#
#  ВАЖНО: сабмодуль src/plugins/external/material-color-utilities НЕ игнорируется
#  (он отслеживается через .gitmodules как gitlink). Здесь глушится только
#  каталог СБОРКИ плагинов, а не каталог external/.
# =============================================================================

# --- C++ plugin build output (cmake -B build -S . -G Ninja ...) ---------------
/src/plugins/build/
# на случай сборки в другом месте — любой каталог сборки:
build/
cmake-build-*/

# --- CMake generated artifacts (если кто-то собрал in-source) -----------------
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
install_manifest.txt
CTestTestfile.cmake
compile_commands.json
_deps/

# --- Ninja --------------------------------------------------------------------
.ninja_deps
.ninja_log
build.ninja
rules.ninja

# --- Скомпилированные бинарники / объектники ----------------------------------
*.o
*.obj
*.so
*.so.*
*.a
*.dylib
*.dll

# --- Qt / QML кэши -------------------------------------------------------------
*.qmlc
*.jsc
*.qsb          # скомпилированные шейдеры (в проекте отсутствуют; глушим генерируемые)
moc_*.cpp
qrc_*.cpp
ui_*.h

# --- Артефакты разработки в репозитории (запрет №28 в AGENTS.md) --------------
shell.zip
*.zip
test.diff
*.diff
*.patch
*.orig
*.rej

# --- Редакторы / IDE ----------------------------------------------------------
.idea/
.vscode/
*.swp
*.swo
*~
.*.kate-swp

# --- ОС -----------------------------------------------------------------------
.DS_Store
Thumbs.db

# --- Claude Code (локальные настройки агента) ---------------------------------
# Общий .claude/settings.json коммитить можно; личный — нет:
.claude/settings.local.json

# --- Логи ---------------------------------------------------------------------
*.log
